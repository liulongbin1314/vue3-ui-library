import { componentInstall } from '@ui-library/utils'
import Collapse from './src/collapse.vue'
import CollapseItem from './src/collapse-item.vue'

export const ACollapse = componentInstall(Collapse)
export const ACollapseItem = componentInstall(CollapseItem)

export default { ACollapse, ACollapseItem }
